// /public/js/users.js
// Mock de alta rápida de eventos en cliente (lista en la tabla). Sin backend.

document.addEventListener('DOMContentLoaded', () => {
    const $ = (id) => document.getElementById(id);
});