<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Skyport — Inicio</title>
  <link rel="icon" href="/img/logoSkyPort.png" type="image/png">

  <!-- Bootstrap CSS (CDN) -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Fredoka+One&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

  <!-- Main stylesheet -->
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <!-- NAVBAR: logo a la izquierda, menú a la derecha -->
  <header class="site-header">
    <nav class="navbar navbar-expand-lg fixed-top">
      <div class="container-fluid">
        <a class="navbar-brand d-flex align-items-center" href="/">
          <img src="/img/logoSkyPort.png" alt="Skyport logo" class="logo-img" style="height:75px;">
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainMenu" aria-controls="mainMenu" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-end" id="mainMenu">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link btn-sky me-2 rounded" href="/login">Iniciar sesión</a>
            </li>
            <li class="nav-item">
              <a class="nav-link btn-sky me-2 rounded" href="/register">Registro</a>
            </li>
            <li class="nav-item">
                <a class="nav-link btn-outline-sky me-2 rounded" href="/guest">Invitado</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <div style="height: 90px;"></div>
  </header>

  <!-- MAIN LAYOUT: tres columnas -->
  <main class="container-fluid mt-4">
    <div class="row gx-4">
      <!-- COLUMNA IZQUIERDA -->
      <aside class="col-lg-3 col-md-4 left-col">
        <div class="card mb-3 news-card">
          <div class="card-header">
            <h3 class="card-title">Noticias Aeronáuticas</h3>
          </div>
          <div class="card-body news-list">
            <article class="news-item mb-3">
              <h4 class="news-title">Novedades en control de tráfico</h4>
              <p class="news-meta">29 oct 2025 — Autor</p>
              <p class="news-excerpt">Resumen breve de la noticia... <a href="#">Leer más</a></p>
            </article>

            <article class="news-item mb-3">
              <h4 class="news-title">Lanzamiento de nuevo avión</h4>
              <p class="news-meta">24 oct 2025 — Industria</p>
              <p class="news-excerpt">Breve extracto de la noticia... <a href="#">Leer más</a></p>
            </article>
          </div>
        </div> 
      </aside>

      <!-- COLUMNA CENTRAL -->
      <section class="col-lg-6 col-md-8 center-col">
        <div class="hero-section card">
          <div class="card-body">
            <h1 class="hero-title">Bienvenido a Skyport</h1>
            <p class="lead hero-sub">
              Tu aeropuerto personal, tu flota y tus misiones. Construye, gestiona y compite en el cielo.
            </p>

            <div class="features mt-4">
              <div class="feature mb-3">
                <h5>Tu aeropuerto</h5>
                <p>Gestiona pistas, hangares y personal para hacer crecer tu base aérea.</p>
              </div>
              <div class="feature mb-3">
                <h5>Misiones y vuelos</h5>
                <p>Completa misiones, gana recursos y desbloquea aviones nuevos.</p>
              </div>
            </div>

            <p class="mt-3 small">Usa el menú en la parte superior derecha para iniciar sesión, registrarte o jugar como invitado.</p>
          </div>
        </div>

        <div class="card mt-4 more-info">
          <div class="card-body">
            <h4>¿Qué puedo hacer en Skyport?</h4>
            <ul>
              <li>Diseñar y personalizar tu aeropuerto.</li>
              <li>Adquirir y mejorar aviones.</li>
              <li>Participar en eventos y rankings.</li>
              <li>Interactuar con otros jugadores.</li>
              <li>kbv</li>
              <li>Participar en misiones cooperativas.</li>
              <li>Explorar el mundo y descubrir nuevas rutas aéreas.</li>
              <li>Personalizar la apariencia de tu aeropuerto.</li>
              <li>Participar en competiciones y eventos especiales.</li>
              <li>Colaborar con otros jugadores en proyectos conjuntos.</li>
              <li>Crear y gestionar tu propia aerolínea.</li>
              <li>Participar en la economía del juego mediante la compra y venta de acciones.</li>
              <li>Realizar investigaciones para desbloquear nuevas tecnologías.</li>
              <li>Gestionar la logística y el transporte de mercancías.</li>
              <li>Participar en la comunidad y foros de jugadores.</li>
              <li>Recibir recompensas y bonificaciones por logros.</li>

            </ul>
          </div>
        </div>
      </section>

      <!-- COLUMNA DERECHA -->
      <aside class="col-lg-3 col-md-4 right-col">
        <div class="card mb-3 news-card">
          <div class="card-header">
            <h3 class="card-title">Acciones de Ryanair en directo</h3>
          </div>
          <div class="card-body">
            <canvas id="ryanairChart" height="200" style="cursor:pointer;"></canvas>
          </div>
        </div>
      </aside>

      <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
      <script>
      const ctx = document.getElementById('ryanairChart').getContext('2d');

      // Datos iniciales
      let priceData = Array.from({ length: 10 }, () => +(25 + Math.random()).toFixed(2));
      let timeLabels = Array.from({ length: 10 }, (_, i) => `-${10 - i}s`);

      // Crear el gráfico
      const ryanairChart = new Chart(ctx, {
        type: 'line',
        data: {
          labels: timeLabels,
          datasets: [{
            label: 'Precio Ryanair (€)',
            data: priceData,
            borderWidth: 2,
            fill: true,
            tension: 0,
            pointRadius: 3,
            pointHoverRadius: 6,
            segment: {
              borderColor: ctx => {
                const p0 = ctx.p0.parsed.y;
                const p1 = ctx.p1.parsed.y;
                return p1 >= p0 ? 'rgba(0, 200, 0, 0.9)' : 'rgba(200, 0, 0, 0.9)';
              },
              backgroundColor: ctx => {
                const p0 = ctx.p0.parsed.y;
                const p1 = ctx.p1.parsed.y;
                // Sombreado bajo la línea
                return p1 >= p0 ? 'rgba(0, 200, 0, 0.15)' : 'rgba(200, 0, 0, 0.15)';
              }
            }
          }]
        },
        options: {
          responsive: true,
          plugins: {
            legend: { display: false }
          },
          animation: { duration: 500 },
          scales: {
            y: {
              beginAtZero: false,
              ticks: { color: '#083d77' },
              grid: { color: 'rgba(0,0,0,0.05)' }
            },
            x: {
              ticks: { color: '#6c757d' },
              grid: { display: false }
            }
          }
        }
      });

      // Actualizar cada 5 segundos
      setInterval(() => {
        const lastPrice = priceData[priceData.length - 1];
        const newPrice = +(lastPrice + (Math.random() - 0.5) * 0.6).toFixed(2);

        priceData.push(newPrice);
        priceData.shift();

        const now = new Date();
        const label = now.getHours().toString().padStart(2, '0') + ':' +
                      now.getMinutes().toString().padStart(2, '0') + ':' +
                      now.getSeconds().toString().padStart(2, '0');
        timeLabels.push(label);
        timeLabels.shift();

        ryanairChart.data.labels = timeLabels;
        ryanairChart.data.datasets[0].data = priceData;
        ryanairChart.update();
      }, 5000);

      // Redirección al hacer clic
      ctx.canvas.onclick = () => {
        window.open('https://investor.ryanair.com/investors-shareholders/share-price/', '_blank');
      };
      </script>

    </div>
  </main>

  <!-- FOOTER -->
  <footer class="site-footer mt-5">
    <div class="container">
      <div class="row">
        <div class="col text-center">
          <small>© <%= new Date().getFullYear() %> Skyport — Proyecto de videojuego aeronáutico</small>
        </div>
      </div>
    </div>
  </footer>

  <!-- Bootstrap JS bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
